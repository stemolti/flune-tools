<!-- DESIGN.md template — generated by /openflune:design Phase 5.5 -->
<!-- Parameters: <design-file>, <design-path>, <framework>, <pen-file-name> -->

# Design Spec

## Design File

All designs live in `<design-path>/<pen-file-name>`. Use Pencil MCP tools to read/modify — never use `Read` or `Grep` on `.pen` files.

## Screens

| Screen | Node ID | Route | Description |
|--------|---------|-------|-------------|
<!-- Populated from batch_get(patterns: [{name: "Screen/.*"}]) -->

## Components

<!-- Angular variant -->
<!-- IF framework == angular -->
| Component | Node ID | Angular Component | PrimeNG / Custom | Used In |
|-----------|---------|-------------------|------------------|---------|

<!-- React variant -->
<!-- IF framework == react -->
| Component | Node ID | React Component | UI Library | Used In |
|-----------|---------|-----------------|------------|---------|

<!-- Vue variant -->
<!-- IF framework == vue -->
| Component | Node ID | Vue Component | UI Library | Used In |
|-----------|---------|---------------|------------|---------|

<!-- Generic variant (fallback) -->
<!-- IF framework == other -->
| Component | Node ID | Code Component | UI Library | Used In |
|-----------|---------|----------------|------------|---------|

<!-- Populated from batch_get(patterns: [{reusable: true}], readDepth: 2) -->

## Annotations

| Note | Node ID | Topic |
|------|---------|-------|
<!-- Populated from batch_get(patterns: [{name: "Note:.*"}]) -->

## Design Tokens

Variables defined in `<design-path>/<pen-file-name>` — access via `get_variables()`.

### Colors

| Token | Value | CSS Custom Property | Usage |
|-------|-------|---------------------|-------|

### Typography

| Token | Value | CSS Custom Property |
|-------|-------|---------------------|

### Radii

| Token | Value | CSS Custom Property |
|-------|-------|---------------------|

### Spacing

| Token | Value | CSS Custom Property |
|-------|-------|---------------------|

## Naming Conventions

| Layer | Convention | Example |
|-------|-----------|---------|
| Components | `Component/<PascalCase>` | `Component/ExerciseCard` |
| Component states | `Component/<Name><State>` | `Component/MuscleChipActive` |
| Screens | `Screen/<route-name>` | `Screen/training-plan` |
| Screen states/variants | `Screen/<route>/<state>` | `Screen/search-drawer/open` |
| Variables | kebab-case with category prefix | `bg-card`, `text-primary`, `radius-md` |
| Internal nodes | camelCase | `tabLabel`, `dragHandle` |
| Annotation notes | `Note: <topic>` | `Note: Set Interaction Patterns` |

## Workflow

### Design changes
1. Open `<pen-file-name>` in Pencil
2. Find screen by node ID from the table above
3. Use `batch_get(<nodeId>, readDepth: 3)` to inspect structure
4. Use `batch_design` to modify
5. Use `get_screenshot(<nodeId>)` to verify

### Component changes
Modify the component once — all screen instances update automatically via refs.

### Implementation
1. Read screen node via `batch_get(<nodeId>, readDepth: 3)`
2. Read variables via `get_variables()` — map to CSS custom properties
3. Read component structure — map to framework components from the Components table
4. Generate component files following the framework conventions

### Quick lookups
- All screens: `batch_get(patterns: [{name: "Screen/.*"}])`
- All components: `batch_get(patterns: [{reusable: true}])`
- Specific screen: `batch_get(nodeIds: ["<id>"], readDepth: 3)`
